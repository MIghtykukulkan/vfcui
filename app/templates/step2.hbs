<!--
<h1> Choose the Vendor for the anchor : <span class="btn btn-success"> {{selectedAnchor}} </span> </h1>
<div class="list-group">
{{#each vendorList as |item|}}
<p {{action 'chooseVendor' item}}>
    <a href="#" class="list-group-item">

     {{item}}  

    </a>
</p>  
{{/each}}
</div>-->

<h1> Choose the Vendor for the anchor :  </h1>
<div class="list-group">

<p class="list-group-item">
    {{!--{{#if regulator}}  --}}
{{multiselect-checkboxes options=vendorList  selection=selectedvendor onchange=(action 'chooseVendor')}}
{{!--{{/if}}--}}
</p> 
 <p>Vendor list:
       <ul>
        {{#each selectedvendor as |vendorList|}}
           <li>{{vendorList}}</li>
          {{/each}}
       </ul>        
</p>

<button type="button" class="btn btn-primary"{{action 'submitselectvendorlist' selectedvendor }}>
      Submit 
      </button>
{{#if anchor}}
<p>Vendor list:
       <ul>
        {{#each selectedvendor as |vendorList|}}
           <li>{{vendorList}}</li>
          {{/each}}
       </ul>        
</p>


{{/if}}
</div>
